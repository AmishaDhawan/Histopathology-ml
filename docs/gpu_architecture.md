# GPU Architecture

## GPU Memory Hierarchy

*To be written in Session 4.*

This guide will cover:
- GPU memory hierarchy (registers → shared memory → L1/L2 cache → global memory)
- Memory bandwidth analysis for vision models
- Roofline model interpretation
- Practical implications for batch size selection and model optimization
